# Deep Semantic Review — Days 6–10

Decision: curriculum-readiness `PASS` for M13–M30 assigned to this batch, M37, M91, M92, M97 and P10. M20/M21/M30 retain their prior deep review.

Each route now has a distinct worked mechanism: coupon waiting stages; collision product/approximation; Poisson likelihood/information; base-rate counts/loss; covariance counterexample; indicator variance; power arithmetic; p-value reference procedure; multiplicity; paired binary inference; Beta posterior comparison; tail-bound selection; bootstrap failure; pass-at-k counting; flake correlation; canary attribution; repeated-success root; Simpson weighting; incident evidence-to-kill; and length-biased sampling.

The independent evaluator and rubric for each ID require a changed distribution, dependence, observation, loss or reliability condition. Review confirms these are mechanism-changing transfers rather than renamed benchmark questions. Learner performance remains `PENDING`.
