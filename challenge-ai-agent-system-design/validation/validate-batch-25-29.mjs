import { read, result } from "./lib.mjs";
const out=result("rebuild-days-25-29"),lessons=read("LESSONS.md"),plan=read("PLAN.md"),assessments=read("ASSESSMENTS.md"),keys=read("ASSESSMENT_KEYS.md"),coverage=read("COVERAGE_MAP.md");
const ids=["M90","M93","M99","P6","P7","P17","M75","M77","M78","M94","M100","P8","M85","P1","P2","P3","P4","P9","P13","P16","P18"],p2=new Set(["M77"]),functions=["Outcome and learning tree","Prerequisites and earlier retrieval","First principles and worked reconstruction","Independent practice","Material transfer and advanced variant","Evaluator, feedback and retrieval","Done and evidence"];
function section(text,level,day){const h="#".repeat(level),s=text.search(new RegExp(`^${h} [^\\r\\n]* ${day} [^\\r\\n]*$`,"m")),t=s>=0?text.slice(s):"",n=t.slice(1).search(new RegExp(`^${h} [^#\\r\\n]+$`,"m"));return n>=0?t.slice(0,n+1):t;}
for(const day of [25,26,27,28,29]){const b=section(lessons,2,day),m=functions.filter(x=>!b.includes(`### ${x}`));m.length?out.fail(`day:${day}`,`Missing: ${m.join(", ")}`):out.pass(`day:${day}`,"Full daily surface present");const p=section(plan,3,day),f=["Outcome:","Minimum action","Target action","Done when:","Evidence:","Stretch:"].filter(x=>!p.includes(x));f.length?out.fail(`plan:${day}`,`Missing: ${f.join(", ")}`):out.pass(`plan:${day}`,"Actionable daily plan present");}
for(const id of ids){const e=`${p2.has(id)?"P2":"P3"}-${id}`;lessons.includes(id)?out.pass(`lesson:${id}`,"Capability routed"):out.fail(`lesson:${id}`,"Lesson route missing");assessments.includes(e)?out.pass(`evaluator:${id}`,e):out.fail(`evaluator:${id}`,`${e} missing`);keys.includes(`**${e}:**`)?out.pass(`rubric:${id}`,e):out.fail(`rubric:${id}`,`${e} rubric missing`);coverage.includes(`| ${id} | Day`)?out.pass(`coverage:${id}`,"Ledger row present"):out.fail(`coverage:${id}`,"Ledger row missing");}
out.pending("workload-pilot","Day 27 has a designed 240-minute budget; no observed pilot was evaluated");out.pending("learner-evidence","No learner attempts or retained-transfer results were evaluated");out.finish();
